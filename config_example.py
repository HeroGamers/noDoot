# RENAME THIS FILE TO "config.py" AFTER EDITING IT!
import os

os.environ["token"] = "TOKEN" # Insert token of bot here
os.environ["prefix"] = "nD!" # What prefix should be used for the commands
os.environ["log"] = "CHANNEL ID" # What channel should the bot paste it logs into?
os.environ["nDGuild"] = "GUILD ID" # What guild can people join to verify themselves alternatively?
os.environ["verificationChannel"] = "CHANNEL ID" # The channel at which the "Click here to start verification", will be
